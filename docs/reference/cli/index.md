# CLI reference

This section is generated from `cox --help` when the docs build has
`coxswain-tools` installed (see `docs/_cli.py`). Locally, install
`coxswain-tools` and run `uv run python docs/_cli.py` to populate it with
one page per command group.
